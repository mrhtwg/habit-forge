org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Force the Gradle JVM to OpenJDK 21 (Gradle 8.14 supports Java 1.8-24; the
# Android Studio embedded JBR is Java 25 and is rejected by Gradle).
# Machine-specific path: adjust if your JDK 21 lives elsewhere, or remove this
# line and pick a JDK 21 in Android Studio Settings > Build Tools > Gradle.

android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
