import 'package:flutter/material.dart';

/// HabitForge bright cartoon color palette (corresponds to docs/ui-mockups/habitforge-cartoon/theme.css)
class AppColors {
  // ── Background / Surface ──
  static const scaffold = Color(0xFFFFF6E7); // Warm cream base
  static const surface = Color(0xFFFFFFFF); // Card white
  static const elevated = Color(0xFFFFEFD6); // Deep cream
  static const border = Color(0xFF3A2A4E); // Deep purple stroke (ink)

  // ── Text ──
  static const textPrimary = Color(0xFF3A2A4E); // ink
  static const textSecondary = Color(0xFF6E5B8A);
  static const textMuted = Color(0xFF8A7CAE);

  // ── Brand / Class Colors ──
  static const primary = Color(0xFF9B6BFF); // violet (mage)
  static const primaryLight = Color(0xFFB455FF);
  static const primaryDark = Color(0xFF7B4FE0);

  // ── Functional Colors ──
  static const gold = Color(0xFFFFC93D); // Sunny gold
  static const goldDark = Color(0xFFF5A60C);
  static const green = Color(0xFF44C86F); // Complete / ranger
  static const greenDark = Color(0xFF2EA758);
  static const red = Color(0xFFFF6B6B); // HP / danger
  static const coral = Color(0xFFFF6B6B);
  static const coralDark = Color(0xFFE84C56);
  static const pink = Color(0xFFF1A6B7);
  static const success = Color(0xFF44C86F);
  static const warning = Color(0xFFFF9800);
  static const error = Color(0xFFE84C56);
  static const info = Color(0xFF4FA3F7);

  // ── Sky / Decorations ──
  static const sky = Color(0xFF9ED9FF);
  static const skyLight = Color(0xFFDBF1FF);
  static const violetLight = Color(0xFFE9DEFF);
  static const greenLight = Color(0xFFE8FFEE);
  static const goldLight = Color(0xFFFFF1C4);
  static const redLight = Color(0xFFFFECEC);

  // ── Skeleton Screens ──
  static const shimmerBase = Color(0xFFF0E7D6);
  static const shimmerHighlight = Color(0xFFFBF5EA);

  AppColors._();
}
